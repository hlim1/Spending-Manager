<div class="add-edit-account">
  <div class="title-bar">
    <h1>New Account</h1>
  </div>

  <div class="form-body">
    <%= render 'form', account: @account %>
    <%= link_to 'Detail', accounts_path %> |
    <%= link_to 'Back', request.referer.present? ? request.referer : default_path %>
  </div>
</div>
