<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Financial_Institution %>
    <%= form.text_field :name, id: :account_name, placeholder: "ex) Chase" %>
  </div>

  <div class="field">
    <%= form.label :Account_Number %>
    <%= form.text_field :accountNumber, id: :account_accountNumber, placeholder: "Last 4-digits. ex) 1234" %>
  </div>

  <div class="actions">
    <%= form.submit "Add", :class => "submit-btn" %>
  </div>
<% end %>
